Instructions:

Used npm, run `npm install` to install all dependencies.

To run tests, run `npm test`.

To run the code run `node . <input file>`.
The two base cases are testCasePart1.txt and testCasePart2.txt
